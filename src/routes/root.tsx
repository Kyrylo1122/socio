import { Box } from "@mui/joy";
import { Header, Body } from "../Components";

const Root = () => {
  return (
    <Box sx={{ width: "100%" }}>
      <Header />
      <Body />
    </Box>
  );
};

export default Root;
